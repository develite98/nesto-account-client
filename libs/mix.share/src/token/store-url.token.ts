import { InjectionToken } from '@angular/core';

export const GET_THEME_URL = new InjectionToken<string>(
  'Mixcore theme list on store'
);
export const GET_APP_URL = new InjectionToken<string>(
  'Mixcore app list on store'
);
